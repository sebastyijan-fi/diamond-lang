# Test conventions

This project stores most regression suites with the transpiler implementation during active development:
- `src/transpiler/*_tests.py`

The `tests/` directory is reserved for user-facing acceptance and package-level suites.
Add external suites here once the public package boundary stabilizes.
