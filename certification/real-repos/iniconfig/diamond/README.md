# Diamond Port Workspace (`iniconfig`)

Place Phase B2 Diamond source files here.

Suggested initial split:

- `parse_dm.dm` for `_parse` semantics
- `errors_dm.dm` for `ParseError`
- `api_dm.dm` for `IniConfig` wrappers

No conversion files are committed yet; this folder is the staging area for the second real-project port.
