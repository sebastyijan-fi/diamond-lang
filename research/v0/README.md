# v0 Historical Notes

This folder contains early scratch outputs created before the current design reset.

Canonical working document:
- `docs/language/LANGUAGE_DESIGN.md` at repository root.

Use this folder only as optional input examples for token measurements.
Do not treat the syntax in `http_handler.dm` as a language decision.
