# Corpus Tests

Use this folder for executable equivalence checks.

Recommended pattern:

- one test module per benchmark program
- assert behavior of Python reference
- later assert transpiled Diamond output matches the same behavior
