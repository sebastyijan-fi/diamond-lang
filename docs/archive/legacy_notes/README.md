# Legacy Notes Archive

This directory stores historical notes and experimental design drafts that are no longer part of the active V1 corpus.

Conventions:
- Keep files read-only.
- Prefix legacy files with date tags when newly added.
- Update `docs/INDEX.md` if a legacy artifact should stay discoverable from the main index.

Active contracts and implementation references continue to live in the live docs tree:
- `docs/decisions/profile_v1/`
- `docs/completeness/`
- `docs/START_HERE.md`
